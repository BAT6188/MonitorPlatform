<Page x:Class="MonitorPlatform.Pages.BossLeft"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      mc:Ignorable="d" 
      xmlns:local="clr-namespace:MonitorPlatform.Convert" 
       xmlns:chart="clr-namespace:De.TorstenMandelkow.MetroChart;assembly=De.TorstenMandelkow.MetroChart" 
	Title="BossLeft">
    <Page.Resources>
        <local:StringToBrushConverter x:Key="StringToBrushConverter" />
    </Page.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="260"></RowDefinition>
            <RowDefinition Height="*"></RowDefinition>
        </Grid.RowDefinitions>

        <Border Grid.Row="0">
            <DockPanel>
                <Border HorizontalAlignment="Left" Style="{StaticResource BossContainer}"  >
                    <StackPanel Height="260" Name="stackPanel3" Width="218"  >
                        <Border BorderBrush="Silver" BorderThickness="1" Height="27" Name="border1">
                            <Label Content="1号线 共有24站" Height="22" Name="label1" />
                        </Border>
                        <StackPanel Orientation="Horizontal">
                            <StackPanel Height="98" Name="stackPanel4"   >
                                <Label Content="上行: 钟南街－木渎" Name="label10" />
                                <Label Content="首发时间: 06:00" Name="label11" />
                                <Label Content="末发时间: 22:45" Name="label12" />
                                <Label Content="当前运行车辆数: 5" Name="label13" />
                            </StackPanel>
                            <Rectangle Height="97" Name="rectangle3" Stroke="Black" Width="1" Canvas.Left="109" Canvas.Top="26" Fill="Black" />
                            <StackPanel Height="98" Name="stackPanel5" >
                                <Label Content="下行: 木渎－钟南街" Name="label24" />
                                <Label Content="首发时间: 06:00" Name="label25" />
                                <Label Content="末发时间: 22:45" Name="label26"  />
                                <Label Content="当前运行车辆数: 5" Name="label27" />
                            </StackPanel>
                        </StackPanel>
                        <Border BorderBrush="Silver" BorderThickness="1" Height="27" Name="border2">
                            <Label Content="2号线 共有22站" Height="22" Name="label2" />
                        </Border>
                        <StackPanel Orientation="Horizontal">
                            <StackPanel Height="98" Name="stackPanel6"   >
                                <Label Content="上行: 钟南街－木渎" Name="label14" />
                                <Label Content="首发时间: 06:00" Name="label15" />
                                <Label Content="末发时间: 22:45" Name="label16" />
                                <Label Content="当前运行车辆数: 5" Name="label17" />
                            </StackPanel>
                            <Rectangle Height="97" Name="rectangle4" Stroke="Black" Width="1"  Fill="Black" />
                            <StackPanel Height="98" Name="stackPane26" Grid.Column="1" >
                                <Label Content="上行: 钟南街－木渎" Name="label20" />
                                <Label Content="首发时间: 06:00" Name="label21" />
                                <Label Content="末发时间: 22:45" Name="label22" />
                                <Label Content="当前运行车辆数: 5" Name="label23" />
                            </StackPanel>
                        </StackPanel>
                    </StackPanel>
                </Border>
                <Border Style="{StaticResource BossContainer}" >
                    <Image Height="255" Name="image1" Stretch="Fill"   Source="/MonitorPlatform;component/Theme/line.bmp" />
                </Border>
            </DockPanel>
        </Border>
        <Border Grid.Row="1">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="2*"></ColumnDefinition>
                    <ColumnDefinition Width="*"></ColumnDefinition>
                    <ColumnDefinition Width="*"></ColumnDefinition>
                </Grid.ColumnDefinitions>
                <Border Grid.Column="0" Style="{StaticResource BossContainer}" >
                    <DockPanel LastChildFill="True">
                        <Grid  DockPanel.Dock="Left">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="25"></RowDefinition>
                                <RowDefinition Height="*"></RowDefinition>
                                <RowDefinition Height="*"></RowDefinition>
                            </Grid.RowDefinitions>
                            <Label Grid.Row="0"  Content="线路流量即时分布" HorizontalAlignment="Center"></Label>
                            <chart:RadialGaugeChart Grid.Row="1"   HorizontalAlignment="Left"
                                Palette="{Binding Path=SelectedPalette.Value}"
                                FontSize="{Binding Path=SelectedFontSize}"
                                ChartTitleVisibility="Hidden"
                                ChartLegendVisibility="Hidden"
                                IsRowColumnSwitched="{Binding Path=IsRowColumnSwitched}"
                                ToolTipFormat="{Binding Path=ToolTipFormat}"
                                Foreground="{Binding Path=Foreground}" 
                                Background="{Binding Path=Background}"                 
                                SelectedItem="{Binding Path=SelectedItem, Mode=TwoWay}"
                                SelectedBrush="{Binding Path=SelectedBrush, Converter={StaticResource StringToBrushConverter}}" Canvas.Left="10" Canvas.Top="56" >
                                <chart:RadialGaugeChart.Series>
                                    <chart:ChartSeries
                                        SeriesCaptionVisibility ="Hidden"
                        SeriesTitle="Errors"
                        DisplayMember="Category"
                        ValueMember="Number"
                        ItemsSource="{Binding Path=FirstLine}">
                                    </chart:ChartSeries>
                                </chart:RadialGaugeChart.Series>
                            </chart:RadialGaugeChart>
                            <chart:RadialGaugeChart Grid.Row="2" HorizontalAlignment="Left"
                                Palette="{Binding Path=SelectedPalette.Value}"
                                FontSize="{Binding Path=SelectedFontSize}"
                                
                                ChartTitleVisibility="Hidden"
                                ChartLegendVisibility="Hidden"
                                IsRowColumnSwitched="{Binding Path=IsRowColumnSwitched}"
                                ToolTipFormat="{Binding Path=ToolTipFormat}"
                                Foreground="{Binding Path=Foreground}" 
                                Background="{Binding Path=Background}"                 
                                SelectedItem="{Binding Path=SelectedItem, Mode=TwoWay}"
                                SelectedBrush="{Binding Path=SelectedBrush, Converter={StaticResource StringToBrushConverter}}" Canvas.Left="10" Canvas.Top="56" >
                                <chart:RadialGaugeChart.Series>
                                    <chart:ChartSeries
                                        SeriesCaptionVisibility ="Hidden"
                        SeriesTitle="Errors"
                        DisplayMember="Category"
                        ValueMember="Number"
                        ItemsSource="{Binding Path=SecondLine}">
                                    </chart:ChartSeries>
                                </chart:RadialGaugeChart.Series>
                            </chart:RadialGaugeChart>
                        </Grid>

                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="*">
                                </RowDefinition>
                                <RowDefinition Height="*">
                                </RowDefinition>
                            </Grid.RowDefinitions>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="30">
                                </ColumnDefinition>
                                <ColumnDefinition Width="*"></ColumnDefinition>
                            </Grid.ColumnDefinitions>

                            <Border Grid.Row="0"  Grid.Column="0" >

                                <TextBlock  TextWrapping="Wrap"  VerticalAlignment="Center"  Width="15"  TextAlignment="Center">1号线站点即时流量</TextBlock>
                            </Border>

                            <Border Grid.Row="0"  Grid.Column="1" >
                                <chart:ClusteredColumnChart 
                                            Palette="{Binding Path=SelectedPalette.Value}"
                                            FontSize="{Binding Path=SelectedFontSize}"
                                            IsRowColumnSwitched="{Binding Path=IsRowColumnSwitched}"
                                            ChartTitleVisibility="Hidden"
                                            ToolTipFormat="{Binding Path=ToolTipFormat}"
                                            Foreground="{Binding Path=Foreground}" 
                                            Background="{Binding Path=Background}"                 
                                            SelectedItem="{Binding Path=SelectedItem, Mode=TwoWay}"
                                            SelectedBrush="{Binding Path=SelectedBrush, Converter={StaticResource StringToBrushConverter}}" >
                                    <chart:ClusteredColumnChart.Series>
                                        <chart:ChartSeries
                                                SeriesTitle="Errors"
                                                DisplayMember="Category"
                                                ValueMember="Number"
                                                ItemsSource="{Binding Path=Errors}" />
                                    </chart:ClusteredColumnChart.Series>
                                </chart:ClusteredColumnChart>

                            </Border>

                            <Border Grid.Row="1"  Grid.Column="0">
                                <TextBlock TextWrapping="Wrap"  VerticalAlignment="Center"  Width="10" TextAlignment="Center">2号线站点即时流量</TextBlock>
                            </Border>

                            <Border Grid.Row="1"  Grid.Column="1" >
                                <chart:ClusteredColumnChart 
                                            Palette="{Binding Path=SelectedPalette.Value}"
                                            FontSize="{Binding Path=SelectedFontSize}"
                                            IsRowColumnSwitched="{Binding Path=IsRowColumnSwitched}"
                                            ChartTitleVisibility="Hidden"
                                            ToolTipFormat="{Binding Path=ToolTipFormat}"
                                            Foreground="{Binding Path=Foreground}" 
                                            Background="{Binding Path=Background}"                 
                                            SelectedItem="{Binding Path=SelectedItem, Mode=TwoWay}"
                                            SelectedBrush="{Binding Path=SelectedBrush, Converter={StaticResource StringToBrushConverter}}" >
                                    <chart:ClusteredColumnChart.Series>
                                        <chart:ChartSeries
                                                SeriesTitle="Errors"
                                                DisplayMember="Category"
                                                ValueMember="Number"
                                                ItemsSource="{Binding Path=Errors}" />
                                    </chart:ClusteredColumnChart.Series>
                                </chart:ClusteredColumnChart>

                            </Border>
                        </Grid>
                    </DockPanel>

                </Border>

                <Border Style="{StaticResource BossContainer}" Grid.Column="1">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="25"></RowDefinition>
                            <RowDefinition Height="25"></RowDefinition>
                            <RowDefinition Height="*"></RowDefinition>
                            <RowDefinition Height="25"></RowDefinition>
                            <RowDefinition Height="*"></RowDefinition>
                        </Grid.RowDefinitions>
                        <Border Grid.Row="0" HorizontalAlignment="Center" VerticalAlignment="Center">
                            <Label Content="设施设备即时信息"></Label>
                        </Border>
                        <Border Grid.Row="1">
                            <Label Content="1号线主要设备情况"></Label>
                        </Border>
                        <Border Grid.Row="2">
                            <chart:ClusteredBarChart     
                                    Palette="{Binding Path=SelectedPalette.Value}"
                                    FontSize="{Binding Path=SelectedFontSize}"
                                    IsRowColumnSwitched="{Binding Path=IsRowColumnSwitched}"
                                    ChartTitleVisibility="Hidden"
                                    ToolTipFormat="{Binding Path=ToolTipFormat}"
                                    Foreground="{Binding Path=Foreground}" 
                                    Background="{Binding Path=Background}"                 
                                    SelectedItem="{Binding Path=SelectedItem, Mode=TwoWay}"
                                    SelectedBrush="{Binding Path=SelectedBrush, Converter={StaticResource StringToBrushConverter}}" >
                                <chart:ClusteredBarChart.Series>
                                    <chart:ChartSeries
                                            SeriesTitle="Errors"
                                            DisplayMember="Category"
                                            ValueMember="Number"
                                            ItemsSource="{Binding Path=Errors}" />
                                </chart:ClusteredBarChart.Series>
                            </chart:ClusteredBarChart>

                        </Border>
                        <Border Grid.Row="3">
                            <Label Content="2号线主要设备情况"></Label>
                        </Border>
                        <Border Grid.Row="4">
                            <chart:ClusteredBarChart     
                                    Palette="{Binding Path=SelectedPalette.Value}"
                                    FontSize="{Binding Path=SelectedFontSize}"
                                    IsRowColumnSwitched="{Binding Path=IsRowColumnSwitched}"
                                    ChartTitleVisibility="Hidden"
                                    ToolTipFormat="{Binding Path=ToolTipFormat}"
                                    Foreground="{Binding Path=Foreground}" 
                                    Background="{Binding Path=Background}"                 
                                    SelectedItem="{Binding Path=SelectedItem, Mode=TwoWay}"
                                    SelectedBrush="{Binding Path=SelectedBrush, Converter={StaticResource StringToBrushConverter}}" >
                                <chart:ClusteredBarChart.Series>
                                    <chart:ChartSeries
                                            SeriesTitle="Errors"
                                            DisplayMember="Category"
                                            ValueMember="Number"
                                            ItemsSource="{Binding Path=Errors}" />
                                </chart:ClusteredBarChart.Series>
                            </chart:ClusteredBarChart>

                        </Border>
                    </Grid>

                </Border>
                <Border Style="{StaticResource BossContainer}" Grid.Column="2">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="25"></RowDefinition>
                            <RowDefinition Height="25"></RowDefinition>
                            <RowDefinition Height="*"></RowDefinition>
                            <RowDefinition Height="25"></RowDefinition>
                            <RowDefinition Height="*"></RowDefinition>
                        </Grid.RowDefinitions>
                        <Border Grid.Row="0" HorizontalAlignment="Center" VerticalAlignment="Center">
                            <Label Content="异常提醒信息"></Label>
                        </Border>
                        <Border Grid.Row="1">
                            <Label Content="1号线"></Label>
                        </Border>
                        <Border Grid.Row="2">
                            <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="*"></RowDefinition>
                                    <RowDefinition Height="*"></RowDefinition>
                                </Grid.RowDefinitions>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition  Width="*"></ColumnDefinition>
                                    <ColumnDefinition  Width="*"></ColumnDefinition>
                                    <ColumnDefinition  Width="*"></ColumnDefinition>
                                </Grid.ColumnDefinitions>

                                <Border   Style="{StaticResource NotifyContainer}"   Grid.Column="0" Grid.Row="0" Grid.RowSpan="2"  >
                                    <TextBlock   HorizontalAlignment="Center" VerticalAlignment="Center">列车位置</TextBlock>
                                </Border>
                                <Border Style="{StaticResource NotifyContainer}" Grid.Column="1" Grid.Row="0"   >
                                    <TextBlock   HorizontalAlignment="Center" VerticalAlignment="Center">客流</TextBlock>
                                </Border>
                                <Border Style="{StaticResource NotifyContainer}" Grid.Column="2" Grid.Row="0"  Background="Red"    >
                                    <TextBlock   HorizontalAlignment="Center" VerticalAlignment="Center">事件</TextBlock>
                                </Border>
                                <Border Style="{StaticResource NotifyContainer}" Grid.Column="1" Grid.Row="1"   >
                                    <TextBlock   HorizontalAlignment="Center" VerticalAlignment="Center">设施设备</TextBlock>
                                </Border>
                                <Border Style="{StaticResource NotifyContainer}" Grid.Column="2" Grid.Row="1"    >
                                    <TextBlock   HorizontalAlignment="Center" VerticalAlignment="Center">环境状况</TextBlock>
                                </Border>
                            </Grid>
                        </Border>
                        <Border Grid.Row="3">
                            <Label Content="2号线"></Label>
                        </Border>
                        <Border Grid.Row="4">
                            <Grid >
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="*"></RowDefinition>
                                    <RowDefinition Height="*"></RowDefinition>
                                </Grid.RowDefinitions>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition  Width="*"></ColumnDefinition>
                                    <ColumnDefinition  Width="*"></ColumnDefinition>
                                    <ColumnDefinition  Width="*"></ColumnDefinition>
                                </Grid.ColumnDefinitions>

                                <Border Style="{StaticResource NotifyContainer}"     Grid.Column="0" Grid.Row="0" Grid.RowSpan="2"  >
                                    <TextBlock HorizontalAlignment="Center" VerticalAlignment="Center">列车位置</TextBlock>
                                </Border>
                                <Border Style="{StaticResource NotifyContainer}"  Grid.Column="1" Grid.Row="0"   >
                                    <TextBlock HorizontalAlignment="Center" VerticalAlignment="Center">客流</TextBlock>
                                </Border>
                                <Border Style="{StaticResource NotifyContainer}" Grid.Column="2" Grid.Row="0"    >
                                    <TextBlock HorizontalAlignment="Center" VerticalAlignment="Center">事件</TextBlock>
                                </Border>
                                <Border Style="{StaticResource NotifyContainer}" Grid.Column="1" Grid.Row="1" Background="Red"   >
                                    <TextBlock HorizontalAlignment="Center" VerticalAlignment="Center">设施设备</TextBlock>
                                </Border>
                                <Border Style="{StaticResource NotifyContainer}" Grid.Column="2" Grid.Row="1"  >
                                    <TextBlock HorizontalAlignment="Center" VerticalAlignment="Center">环境状况</TextBlock>
                                </Border>
                            </Grid>
                        </Border>
                    </Grid>

                </Border>
            </Grid>

        </Border>

    </Grid>
</Page>
